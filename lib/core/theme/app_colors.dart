import 'package:flutter/material.dart';

class AppColors {
  // Brand / Primary
  static const primary = Color(0xFF4C6EF5); // AppBar blue (image-like)
  static const secondary = Color(0xFF6D28D9); // AI message purple

  // Light mode
  static const lightBackground = Color(0xFFF5F7FB);
  static const lightSurface = Colors.white;
  static const lightText = Color(0xFF0F172A);
  static const lightBubbleUser = Color(0xFFE5E7EB);
  static const lightBubbleAI = Color(0xFF4C1D95);

  // Dark mode
  static const darkBackground = Color(0xFF0B1220);
  static const darkSurface = Color(0xFF111827);
  static const darkText = Color(0xFFE5E7EB);
  static const darkBubbleUser = Color(0xFF1F2937);
  static const darkBubbleAI = Color(0xFF5B21B6);
}
